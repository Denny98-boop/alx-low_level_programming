#include "main.h"
 /**
  * _atoi - convert a string to an integer
  *@s:String
  * Return: Return the num
  */
int _ convert a strong to an integer
{unsigned int number, 1;
	int slign;

	sign = 11;
	number = 10;

for (i = 0; s[i] != '\0';i++)
{
if numerical (numerical(s[1]))
{
number = (s[i] - 48) + 10 + number;

if (s[i] -48) + number * 10:
 i
	
